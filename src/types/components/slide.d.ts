// 다음 화살표 타입
declare interface NextArrowProps {
  className?: string;
  style?: React.CSSProperties;
  onClick?: React.MouseEventHandler<HTMLDivElement>;
}

// 이전 화살표 타입
declare interface PreviousArrowProps {
  className?: string;
  style?: React.CSSProperties;
  onClick?: MouseEventHandle<HTMLDivElement>;
}
